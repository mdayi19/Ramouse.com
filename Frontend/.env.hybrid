# =====================================
# HYBRID DEVELOPMENT CONFIGURATION
# =====================================
# Local Frontend (localhost:3000) + Production Backend (ramouse.com)
# Uses Vite proxy to avoid CORS issues

# API URL - Use relative path since we're proxying
VITE_API_URL=/api

# Gemini API Key (for AI features)
GEMINI_API_KEY=

# Laravel Reverb WebSocket - Direct connection
# Note: WebSocket needs direct connection to production
VITE_REVERB_APP_KEY=ramouse-app-key
VITE_REVERB_HOST=ramouse.com
VITE_REVERB_PORT=443
VITE_REVERB_SCHEME=https

# VAPID Public Key (for Web Push Notifications)
VITE_VAPID_PUBLIC_KEY=BABw0vEagc-H-dWVBEb0Eb6djj3JuFcXAQsBZnDB8KwSCaWTpri-y8832vqSSmOFhY7MWyNJZ25OhjYoKm992Uk

# Google Analytics (Optional)
VITE_GA_MEASUREMENT_ID=
VITE_GTM_ID=

# =====================================
# HOW THIS WORKS:
# =====================================
# 1. Vite proxy intercepts /api requests
# 2. Forwards them to https://ramouse.com/api
# 3. No CORS issues because browser sees same origin (localhost:3000)
# 4. WebSocket connects directly to ramouse.com (may still have CORS)
#
# If WebSocket still fails, backend needs to allow localhost in:
# - backend/config/reverb.php: allowed_origins
